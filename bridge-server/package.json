{
  "name": "transcription-bridge-server",
  "version": "1.0.0",
  "private": true,
  "description": "REST bridge — proxies tool calls from the agent runner to the Python FastAPI backend",
  "type": "module",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0"
  }
}
